type User {
  _id: ID!
  firstName: String
  lastName: String
  # projects: [Project]
}

# type Project {
#   id: Int!
#   name: String
# }

type Query {
  user(_id: ID!): User
}

type Mutation {
  createUser(firstName: String!, lastName: String!): User
}

# type Mutation {
#   updateFirstName(id: Int!, firstName: String!): User
# }
